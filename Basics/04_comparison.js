// console.log(2>1); // true
// console.log("2">1); // true
// console.log("02">1); // true js automartically convert string to number when we compare with number
// console.log("2"<"12"); // false because when we compare string with string it compare first character of both string and "2" is greater than "1" si it return false

// console.log(null > 0); // false
// console.log(null ==0); // false
// console.log(null >= 0); // true because when we compare null with number it convert null to 0 and 0 is greater than or equal to 0 so it return true but when we compare null with 0 it return false because null is not equal to 0


// console.log(undefined == 0);// false
// console.log(undefined > 0);// false
// console.log(undefined <0); // false


// strict check === checks the value and type of the variable
console.log("2"===2); // false

console.log("110" < "a"); // true because when we compare string with string it compare first character of both string and "1" is less than "a" so it return true

console.log("110" > "a"); // false because when we compare string with string it compare first character of both string and "1" is less than "a" so it return false
